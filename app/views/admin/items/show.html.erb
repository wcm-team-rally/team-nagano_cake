<h1>商品詳細</h1>
<table>
    <tr>
      <td><%= image_tag url_for(@item.image), size: '100x100' %></td>
      <th>商品名</th><td><%= @item.name %></td>
      <th>商品説明</th><td><%= @item.introduction %></td>
      <th>ジャンル</th><td><%= @item.genre_id %></td>
      <th>税抜価格（税抜価格）</th><td><%= number_with_delimiter((@item.price * 1.1).round) %>円 ( <%= number_with_delimiter(@item.price) %>円 )</td>
      <th>販売ステータス</th><td><%= @item.on_sale? ? '販売中' : '販売停止中' %></td>
    </tr>
  <%= link_to "編集する", edit_admin_item_path(@item), class: "btn btn-sm btn-success" %>
</table>